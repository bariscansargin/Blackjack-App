import React from "react";

const PlayingCard = ({value, type}) => {
    return (
    <div className="flex flex-col">
      <div className="w-full"></div>
      <div className=" flex justify-center items-center w-full"></div>
      <div className="w-full"></div>
    </div>
  );
};

export default PlayingCard;
